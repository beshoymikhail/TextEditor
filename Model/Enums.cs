 namespace TextEditor.Model
{
    public enum FunctionType
    {
        Function,
        Lemma,
        Fixpoint,
    }
    public enum SourceFile
    {
        Implementation,
        Specification,
        Aixiliary
    }
}
